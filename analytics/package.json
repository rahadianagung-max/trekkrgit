{
  "name": "trekkr-sabermetrics",
  "version": "0.1.0",
  "private": true,
  "description": "Trekkr Sabermetrics — WOWY contribution + partner-fit analysis for padel (prototype).",
  "scripts": {
    "build": "esbuild src/app.jsx --bundle --minify --format=iife --outfile=app.js",
    "dev": "esbuild src/app.jsx --bundle --format=iife --outfile=app.js --servedir=. --watch"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.23.0"
  }
}
